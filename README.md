# introduction

using ubuntu on laptop with dodgy track pad!

aim is to use asynchronous programming on tpbot using bbc microbit v2

# installing vs code

      as root
      change to software folder

      wget -O vscode.deb "https://code.visualstudio.com/sha/download?build=stable&os=linux-deb-x64"
      dpkg -i /home/robotuser/software/vscode.deb
      apt install -f    # pulls in any missing dependencies

# added robotuser into docker group
